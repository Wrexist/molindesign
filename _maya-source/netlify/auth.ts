import {createHmac,timingSafeEqual,randomBytes} from 'node:crypto';
export function equal(a:string,b:string){const x=Buffer.from(a),y=Buffer.from(b);return x.length===y.length&&timingSafeEqual(x,y);}
export function issue(secret:string,now=Date.now()){const payload=Buffer.from(JSON.stringify({exp:now+28800000,id:randomBytes(16).toString('hex')})).toString('base64url');return payload+'.'+createHmac('sha256',secret).update(payload).digest('base64url');}
export function valid(token:string,secret:string,now=Date.now()){try{const [payload,signature,...rest]=token.split('.');if(rest.length||!signature||!equal(signature,createHmac('sha256',secret).update(payload).digest('base64url')))return false;const value=JSON.parse(Buffer.from(payload,'base64url').toString());return typeof value.exp==='number'&&value.exp>now&&value.exp<=now+28800000;}catch{return false;}}
export function cors(req:Request){const origin=req.headers.get('origin');const allowed=['https://wrexist.github.io','https://maya-haglund-admin.netlify.app'];if(origin&&!allowed.includes(origin))return null;return {'Access-Control-Allow-Origin':origin||allowed[0],'Access-Control-Allow-Headers':'Content-Type, Authorization','Access-Control-Allow-Methods':'GET, POST, PUT, DELETE, OPTIONS','Vary':'Origin','Cache-Control':'no-store','X-Content-Type-Options':'nosniff'};}
